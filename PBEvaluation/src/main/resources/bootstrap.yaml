spring:
  application:
    name: PBEvaluation
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/Evaluation
    username: postgres
    password: MyPostgre74SQL38
  jpa:
    hibernate:
      ddl-auto: update