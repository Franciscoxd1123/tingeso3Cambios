spring:
  application:
    name: PBRequest
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/Requests
    username: postgres
    password: MyPostgre74SQL38
  jpa:
    hibernate:
      ddl-auto: update